# GitHub fine-grained personal access token
# Scopes needed: Contents (read and write) for this repo only
VITE_GITHUB_TOKEN=
VITE_GITHUB_REPO=your-username/fuel

# Optional: seed fill-ups for local dev (JSON array of Fillup objects, no Firebase calls)
VITE_MOCK_FILLUPS=
